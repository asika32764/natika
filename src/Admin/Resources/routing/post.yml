# Post
post:
    pattern: /post(/id)
    controller: Post
    variables:
        layout: post

# Posts
posts:
    pattern: /posts(/page)
    controller: Posts
    action:
        post: CopyController
        patch: BatchController
        put: FilterController
    variables:
        layout: posts
