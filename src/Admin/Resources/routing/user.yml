# User
user:
    pattern: /user(/id)
    controller: User
    variables:
        layout: user

# Users
users:
    pattern: /users(/page)
    controller: Users
    action:
        post: CopyController
        patch: BatchController
        put: FilterController
    variables:
        layout: users
